name: linux

on:
  push:
    branches: [master]
    tags:    ['v*']
  pull_request:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: write   # 上传 release 资产

# 两个独立 job,各自在原生架构 runner 上跑 scripts/build_flatpak_native.sh:
#   - x86_64:  ubuntu-24.04(原生 amd64)
#   - aarch64: ubuntu-24.04-arm(原生 arm64)
# 脚本要求 --arch 与宿主架构一致;不装 qemu-user-static,也不需要
# gcc-aarch64-linux-gnu 之类的交叉工具链 —— 原生构建用宿主 gcc。

jobs:
  flatpak-x86_64:
    name: flatpak x86_64
    runs-on: ubuntu-24.04
    steps:
      - name: checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # 需要 git describe 取版本号

      - name: setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26.6'
          cache: true

      # 只装原生构建需要的东西:flatpak / flatpak-builder / 宿主 gcc。
      # (脚本会把 flatpak SDK 当 sysroot,GL/X11/wayland 的头与库都在里面,
      # 不需要额外的 -devel 包。)
      - name: install flatpak toolchain
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y \
            flatpak \
            flatpak-builder \
            build-essential \
            pkg-config

      - name: install runtime and SDK
        # 脚本要求目标架构的 SDK/runtime 已就绪;用未过滤的 flathub 远程
        # (dl.flathub.org)保证 ref 齐全,发行版自带的可能是过滤过的。
        run: |
          set -euo pipefail
          flatpak remote-add --if-not-exists flathub \
            https://dl.flathub.org/repo/flathub.flatpakrepo
          flatpak install -y --noninteractive flathub \
            org.gnome.Sdk//50 org.gnome.Platform//50

      - name: build flatpak x86_64
        run: |
          set -euo pipefail
          ./scripts/build_flatpak_native.sh --arch=x86_64

      - name: upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: lgdm-flatpak-x86_64
          path: dist/lgdm-*-x86_64.flatpak
          if-no-files-found: error
          retention-days: 30

  flatpak-aarch64:
    name: flatpak aarch64
    runs-on: ubuntu-24.04-arm   # 原生 arm64 runner
    steps:
      - name: checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26.6'
          cache: true

      - name: install flatpak toolchain
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y \
            flatpak \
            flatpak-builder \
            build-essential \
            pkg-config

      - name: install runtime and SDK
        run: |
          set -euo pipefail
          flatpak remote-add --if-not-exists flathub \
            https://dl.flathub.org/repo/flathub.flatpakrepo
          flatpak install -y --noninteractive flathub \
            org.gnome.Sdk//50 org.gnome.Platform//50

      - name: build flatpak aarch64
        run: |
          set -euo pipefail
          ./scripts/build_flatpak_native.sh --arch=aarch64

      - name: upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: lgdm-flatpak-aarch64
          path: dist/lgdm-*-aarch64.flatpak
          if-no-files-found: error
          retention-days: 30

  release:
    name: release
    needs: [flatpak-x86_64, flatpak-aarch64]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: publish to release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/lgdm-flatpak-x86_64/*.flatpak
            artifacts/lgdm-flatpak-aarch64/*.flatpak
          generate_release_notes: true
          fail_on_unmatched_files: true
